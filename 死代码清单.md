# 死代码清单

扫描范围: `Core/Src` `Core/Inc` `foc/foc_fast` `foc/foc_app`
忽略: `Drivers/` `Middlewares/` `MDK-ARM/`（HAL/CMSIS/Keil 自动生成）

按删除收益从大到小排序（收益 = 行数 × 删除安全度）。

---

## 1. foc_api.c — EtherCAT/CiA402 遗留 getter/setter（约 600 行）

PHU 主从架构里给 EtherCAT SDO 字典暴露的接口层。本工程走 USART1 命令 + CAN 万里扬协议，整层用不上，全是空函数 / `return 0`，编译器多半都能 dead-strip 掉，但占源文件视觉空间。

### 1.1 第一段 (foc_api.c:35-231, 40 函数)

| 行 | 函数 | 备注 |
|----|------|------|
| 35 | `motorRunStateUpdate` | 空 |
| ~ | `get_operation_mode` / `get_actual_position` / `get_actual_velocity` | return 0 |
| ~ | `get_velocity_ref` / `get_velocity_lim` / `get_max_current` / `get_max_speed` | return 0 |
| ~ | `get_actual_current` / `get_current_ref` | return 0 |
| ~ | `postion_limit_check` / `set_postion_ref` / `get_position_ref` | 空 / return 0 |
| ~ | `getControlErrFlag` / `getControlStatusFlag` | return 0 |
| ~ | `motorPhaseCurrentStatesGet` / `motorZeroPointStaGet` | return 0 |
| ~ | `motorSpeedOffsetGet` / `motorSpeedOverGet` / `motorPosOffsetGet` | return 0 |
| ~ | `flashReadErrGet` / `getMotorRunState` | return 0 |
| ~ | `setMotorPhase{U,V,W}Err` / `resetMotorPhase{U,V,W}Err` / `getMotorPhase{U,V,W}Err` | 9 个 phase 故障接口 |
| ~ | `zeroPointLostCheck` / `setCommnicationErr` | 空 |
| ~ | `motorErrReset` / `communicationErrReset` | 空 |
| ~ | `motorRun` / `motorStop` / `motorStopProgress` | 空 |
| 231 | `ServoStateFlagGudge` / `get_motor_arrived_flag` | return 0 |

### 1.2 第二段 (foc_api.c:637-821, 43 函数)

| 行 | 函数 | 备注 |
|----|------|------|
| 637 | `Set_Position_Limit` | 空 |
| ~ | `get_board_temp` | return 0 |
| ~ | `Pid_Control_Enable` / `Get_error_speed` / `set_error_eeprom` | 空 |
| ~ | `Quick_stop_option` / `Shutdown_code` / `Disable_Operation` | 空 |
| ~ | `Halt_Option` / `Fault_React` / `Sensor_select` | 空 |
| ~ | `Speed_max_warn` / `Home_Offset` | 空 |
| ~ | `Set_QuickStop_deceleration` / `Set_Torque_slope` | 空 |
| ~ | `Set_Homing_Method` / `Set_Homing_Speeds` / `Set_Homing_Speeds_zero` / `Set_Homing_Acceleration` | 空 |
| ~ | `Set_Torque_Offset` / `Set_Maximum_acceleration` / `Set_Maximum_deceleration` | 空 |
| ~ | `Set_Positive_torque_limit` / `Set_Negative_torque_limit` | 空 |
| ~ | `Set_Position_Option_Code` | 空 |
| ~ | `Get_Follow_offset` / `Get_Pos_Demand` | return 0 |
| ~ | `Set_Break_Dealy_Time` | 空 |
| ~ | `Get_Motor_Encoder_Value` / `Get_Double_Encoder_Value` | return 0 |
| ~ | `Motion_profile_type` / `Interpolatedsubmodeselect` | 空 |
| ~ | `InterpolationIndex` / `Inter_polation_Period` | 空 |
| ~ | `Get_Supported_Drive_Modes` | return 0 |
| ~ | `set_STO_controll` / `get_STO_status` | 空 / return 0 |
| ~ | `set_system_restart` / `get_Historical_Fault` | 空 / return 0 |
| ~ | `reset_default_value` / `FlashData_recover_default` | 空 |
| 821 | `set_Maxposition_Limit` / `set_Minposition_Limit` | 空 |

---

## 2. foc_bsp.c — HPMicro HPM 平台 stub（22 函数）

HPMicro 移植蓝本里的硬件初始化函数，STM32 走 CubeMX 生成的 MX_*_Init，这一套不需要。全部无调用点。

### 2.1 高收益（多参数空函数）

| 行 | 函数 |
|----|------|
| 82  | `pwmv2_duty_init` (7 参) |
| 111 | `pwmv2_trigfor_adc_init` (8 参) |
| 121 | `pwmv2_trigfor_sei_init` (8 参) |
| 95  | `adc_init_udc_temp` (4 参) |
| 98  | `adc_cfg_init` (5 参) |
| 104 | `init_trigger_cfg` (5 参) |
| 101 | `init_trigger_mux` (3 参) |
| 108 | `adc_module_cfg` (3 参) |

### 2.2 简单空函数

| 行 | 函数 |
|----|------|
| 61  | `seiInterruptReset` |
| 64  | `led_init` |
| 67  | `break_motor_operation_init` |
| 70  | `sto_motor_operation_init` |
| 73  | `isr_gpio` |
| 76  | `pwm_pins_init` |
| 79  | `isr_pwm0_counter` |
| 89  | `bldc_foc_pwmset` |
| 92  | `adc_pins_init` |
| 138 | `adc_isr_enable` |
| 141 | `isr_adc` |
| 144 | `motor_encoder_spi` (return 0) |
| 148 | `get_clock_cpu_ms` (return 0) |
| 152 | `get_ver_id` (return 0) |

---

## 3. 注释掉的代码块

| 文件 | 行 | 行数 | 内容 |
|------|----|------|------|
| `foc/foc_fast/foc_test.c` | 21-50 | 30 | `foc_interfact_test` 整个函数体 |
| `foc/foc_fast/foc_controller.c` | 80-91 | 12 | flash_data 旧 PID 初始化值 |
| `foc/foc_fast/foc_data.c` | 151-158 | 8 | InitFlashData 角度偏置旧逻辑 |
| `foc/foc_fast/foc_api.c` | 625-629 | 4 | autoTunePositionLoopPI 旧参数写入 |

---

## 4. 完全无人调用的非 static 函数

| 文件 | 行 | 函数 | 备注 |
|------|----|------|------|
| `foc/foc_fast/foc_test.c` | 20 | `foc_interfact_test` | 仅头文件声明，函数体已注释 (§3) |

---

## 删除建议（每批一个 commit + 增量编译验 0 Error）

1. **§2 foc_bsp 22 个 stub** —— 最孤立，删完同步清 `foc_bsp.h` 里的对应 prototype
2. **§1.1 / §1.2 foc_api 80+ 占位接口** —— 量大但同类，连 `foc_api.h` 里的对应声明一起处理
3. **§4 + §3 foc_test 整文件 + 三处注释块** —— 收尾清理

每批之间留 commit，万一后面发现某个"占位"其实有外部调用（例如 ifly_fault.c 通过函数指针注册），可以单独 revert 那一项。

## 已确认排除（看起来像死代码但不是）

- `cantest1~19` / `bwtest1~10` 分支 —— 通过字符串命令派发，不会被 grep 调用点找到
- HAL 弱符号 `HAL_*_Callback` —— 由 HAL 库内部调用
- ISR (`*_IRQHandler`) —— 由向量表调用
- `seiInterruptReset` 等 PHU 名字 —— 已确认在 STM32 路径不被调用，归入 §2
