# 机器狗关节模组上位机协议适配方案

> 目标：适配"机器狗关节模组上位机技术协议"，兼容现有万里扬 V1.7 + CAN 调试通道

---

## 核心约束

1. **万里扬协议不能动** — 0x080~0x7FD 帧格式和解析逻辑已测通，机器狗关节控制走的就是这套（MIT 0x500、速度 0x200、位置 0x400）
2. **CAN 调试通道 0x7E0~0x7EF 保持** — foc_tuner 上位机依赖
3. **硬件过滤器全通** — 软件分流，新协议加 case 即可
4. **Flash 已有 node_id 持久化** — SDO 0x2F00 可写

---

## P0-1: MIT 阻抗控制模式面板

**现状**: MCU 侧 0x500 MIT 12B 帧已完整实现（POS24 + VEL16 + T16 + Kp16 + Kd16 + ID8），上位机缺交互面板。

**方案**: 纯上位机改动，MCU 零改动。
- foc_tuner 新增 "MIT Control" Tab
- 滑动条/输入框: p_des(-7~+7 rad), v_des(-20~+20 rad/s), t_ff(-500~+500 Nm), Kp(0~500), Kd(0~20)
- 定时发送: 默认 1ms 周期（MIT 要求 ≤20ms 否则触发看门狗）
- 停止按钮: 发送全零帧（安全停车）
- 单位换算显示: Nm <-> A（走 Kt LUT）

**兼容性**: 100% 兼容，帧格式不变。

---

## P0-2: CAN 通用帧发送面板

**现状**: 上位机只能发结构化命令帧。

**方案**: 纯上位机改动。
- 新增 "Raw CAN" Tab：手动填 CAN ID (hex) + Data (hex) + DLC
- 定时发送: 周期(ms) + 次数 + Data/ID 递增选项
- 帧格式: Classic CAN / CAN-FD / BRS 开关
- 接收区已有 CAN Traffic Panel，加过滤列即可

**兼容性**: 只是发帧工具，不碰协议解析。

---

## P0-3: 多电机管理（12 关节）

最复杂的改动，涉及 MCU + 上位机。

**现状**:
- `s_node_id` 运行时可通过 SDO 0x2F00 改写并持久化
- 0x500 批帧里用 D[11] 区分目标电机
- 0x100+ID 状态帧天然携带 node_id 偏移
- 硬件全通滤波，12 个电机在同一总线上各自匹配自己的 ID

**上位机方案**:
```
+-----------------------------+
|  Joint Selector: ID 1~12    |  <- 下拉/Tab 切换
|  [Scan Bus] [Assign ID]     |
+-----------------------------+
|  当前选中关节的独立面板      |
|  (MIT / PID / 状态 / Flash) |
+-----------------------------+
```

- **Scan Bus**: 发 0x080 广播，解析所有 0x1XX 响应，自动发现在线节点
- **Assign ID**: 写 SDO 0x2F00 修改节点 ID（需要先单节点上电避免冲突）
- **上下文切换**: 切换选中 ID 后，后续所有命令/日志自动绑定该 ID
- **调试通道**: 0x7E0 调试帧加 1B node_id 前缀（MCU 端需小改）

**MCU 改动（最小化）**:
- `can_debug_rx_isr()` 增加 node_id 匹配（D[0] == my_id 才处理），向下兼容（D[0]==0xFF 广播模式走原逻辑）
- 调试响应帧 0x7E1/0x7E2/0x7E3 头部加 1B node_id

**兼容策略**:
- 协议版本号 bump（PING 响应已带版本字段）
- 旧上位机不发 node_id 前缀 -> MCU 检测帧长判断新/旧格式
- 新上位机检测 PING 版本号，决定是否启用多 ID 模式

---

## P1: CANFD 采样点 + 报文长度配置

纯上位机。python-can `Bus()` 已支持 `bitrate_switch`、`data_bitrate`，Settings 面板暴露参数。MCU 侧 CANFD 波特率运行时改写需加 SDO index（可选）。

---

## P1: 硬件型号 -> 参数映射

- MCU 端 VERSION 响应帧增加硬件型号字段（2B 型号码）
- 上位机维护 JSON 配置表：型号 -> {NPP, 减速比, 额定电流, Kt, 额定速度, ...}
- 连接后自动查询，面板预填参数

**MCU 改动**: version 命令响应结构扩 2 字节。

---

## P1: 位置速度联合模式

MCU 已有 CSP 模式（位置环+梯形规划+速度限幅），串口命令 `RuncmdXMYtarZ` 可设 mode=CSP。上位机加面板即可，MCU 无改动。

---

## P2: 零点/限位/抱闸

SDO 已支持，串口命令已有。上位机加按钮走 0x600+ID SDO write。MCU 零改动。

---

## P2: 通信超时检测 + 自动重连

纯上位机。
- 心跳: 定时发 PING (0x7E0)，200ms 无响应标红
- 自动重连: USB-CAN 适配器拔插检测 + Bus reinit
- MCU 侧已有 200ms/20ms 看门狗，天然安全

---

## 改动量矩阵

| 层 | 改动项 | 工作量 |
|---|---|---|
| **MCU** | 调试帧加 node_id 前缀（兼容模式） | 0.5 天 |
| **MCU** | VERSION 响应加硬件型号 | 0.5 天 |
| **MCU** | SDO 新增 CANFD 波特率运行时配置 | 1 天（可选） |
| **上位机** | MIT 阻抗控制面板 | 2 天 |
| **上位机** | 通用帧发送面板 | 1 天 |
| **上位机** | 多电机管理框架 | 3 天 |
| **上位机** | 串口 HEX 收发 + 配置扩展 | 1 天 |
| **上位机** | 参数显示面板 + 型号映射 | 1 天 |
| **上位机** | 零点/限位/抱闸按钮 | 0.5 天 |
| **上位机** | 超时检测 + 重连 | 1 天 |

---

## 推荐实施顺序

```
Phase 1 (MCU不动，上位机先行):
  MIT面板 -> 通用帧发送 -> 串口扩展 -> 超时检测

Phase 2 (MCU小改，多电机支持):
  调试帧 node_id -> 多电机管理 -> VERSION 扩展 -> 参数映射

Phase 3 (收尾):
  零点/限位/抱闸 -> 温度面板 -> 增益范围可配 -> CANFD波特率运行时配置
```

---

## 待确认设计决策

1. **多电机调试通道方案**: 帧长度判断新/旧（简单但脆弱） vs 协议版本协商（稳但要两步握手）？
2. **MIT 面板定时发送**: 走独立线程 1ms 定时 vs 依赖上位机已有的 CAN TX 调度器？
3. **CAN_ID 分配策略**: 出厂默认 ID=1 还是按物理位（LF=1, RF=2, ...）预烧？
4. **上位机技术栈**: 继续 PyQt foc_tuner 扩展，还是新起一个专门的"12关节总控"应用？
