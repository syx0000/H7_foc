# 机器狗关节模组上位机技术协议

> 对照现有 foc_tuner 上位机功能，**标红项为当前未实现的功能**

---

## 一、CANFD通信

实现对CANFD的配置、CANFD发送数据与接收数据

| 序号 | 功能 | 状态 |
|------|------|------|
| 1 | CANFD配置：通道选择、ID波特率、ID采样点、DATA波特率、DATA采样点、数据报文长度1~64字节可调 | **<font color=red>部分缺失：采样点配置、报文长度可调未实现</font>** |
| 2 | CANFD发送：发送数据配置（数据帧ID(HEX)、数据报文长度、数据区(HEX)）、帧格式选择功能、定时发送功能（发送次数、发送周期、DATA递增、ID递增） | **<font color=red>未实现：通用帧发送面板、定时发送、递增发送</font>** |
| 3 | CANFD接收：接收数据显示（序号、时间戳、帧格式、ID、状态、数据长度、数据内容） | 已实现（CAN Traffic Panel） |

---

## 二、版本升级（bin文件传输预留串口、CANFD的接口）

读出现有版本，并实现新版本的固件升级

| 序号 | 功能 | 状态 |
|------|------|------|
| 1 | 读取当前电机的硬件型号与软件版本并显示，并可根据硬件型号显示出对应的电机参数 | 部分实现（version命令可读版本，**<font color=red>硬件型号→电机参数自动映射未实现</font>**） |
| 2 | 获取新版本bin文件，并发送到下位机中 | 已实现（OTA升级，串口+CAN双通道） |

---

## 三、编码器校准（优先）

| 序号 | 功能 | 状态 |
|------|------|------|
| 1 | 编码器校准：下发校准高速端编码器指令 | 已实现（Cali命令，Maintenance面板） |

---

## 四、串口通信

实现对串口的配置、串口发送数据与接收数据

| 序号 | 功能 | 状态 |
|------|------|------|
| 1 | 串口配置：串口号、波特率、数据位、校验位、停止位 | 部分实现（串口号+波特率已有，**<font color=red>数据位/校验位/停止位配置未实现</font>**） |
| 2 | 串口的打开与关闭 | 已实现 |
| 3 | 串口发送：格式选择（ASCII/HEX）、普通发送与队列发送功能 | 部分实现（ASCII发送已有，**<font color=red>HEX格式发送、队列发送未实现</font>**） |
| 4 | 串口接收：格式选择（ASCII/HEX）、可选择是否显示接收数据 | 部分实现（ASCII显示已有，**<font color=red>HEX格式显示、显示开关未实现</font>**） |

---

## 五、参数显示

各种参数的读取并显示

| 序号 | 功能 | 状态 |
|------|------|------|
| 1 | 电机参数：相电阻、相电感、磁链、转动惯量、转矩常数 | 部分实现（bwtest3/4/5可辨识Rs/Ld/Lq/ψ/J并打印，**<font color=red>无独立参数显示面板、转矩常数Kt未实现</font>**） |

---

## 六、下发控制指令与调试

设置各种模式的控制参数并发送控制指令，并获取电机的当前状态，在曲线图中显示

| 序号 | 功能 | 状态 |
|------|------|------|
| 1 | CAN_ID的设定 | **<font color=red>未实现：运行时CAN节点ID修改</font>** |
| 2 | MIT模式参数设定（填空与滑动条）：位置给定、速度给定、Kp、Kd、转矩给定 | **<font color=red>未实现：MIT阻抗控制模式面板</font>** |
| 3 | 位置速度模式参数设定：位置给定、速度限定、位置环Kp与Ki、速度环Kp与Ki | 部分实现（位置模式+PID调参已有，**<font color=red>位置速度联合模式面板、速度限定配置未实现</font>**） |
| 4 | 速度模式参数设定：速度给定、速度环Kp与Ki | 已实现（Motor Control + PID Panel） |
| 5 | 普通发送与定时发送功能（ms级） | **<font color=red>未实现：通用定时发送功能</font>** |
| 6 | 实时位置、速度、力矩值、电流、电压的曲线图显示，通过复选框选取显示哪些 | 已实现（Waveform Widget，多通道+复选框） |

---

## 七、建议补充功能（协议未列但实际必需）

| 序号 | 功能 | 当前状态 | 必要性 |
|------|------|----------|--------|
| 1 | **<font color=red>故障诊断与保护</font>** — 故障码读取/清除、保护状态显示、故障历史 | 已实现（Fault Panel） | 高 |
| 2 | **<font color=red>Flash参数管理</font>** — 写入/擦除/恢复出厂/RAM vs Flash对比 | 已实现（Flash Panel） | 高 |
| 3 | **<font color=red>PID AutoTune</font>** — 三环自动整定 | 已实现（bwtest6/7/8） | 高 |
| 4 | **<font color=red>带宽测试+Bode图</font>** — 频率响应扫描 | 已实现（Bandwidth Panel） | 中 |
| 5 | **<font color=red>温度监控</font>** — 板温/电机温度实时显示+报警阈值 | 部分（logid165显示板温，无独立面板） | 中 |
| 6 | **<font color=red>抱闸控制</font>** — 开/关/延时配置 | 固件已支持，上位机无独立控制 | 高 |
| 7 | **<font color=red>位置限位设定</font>** — 正/负限位角度+使能开关 | SDO已支持，上位机无配置面板 | 高 |
| 8 | **<font color=red>零点设置</font>** — 当前位置设零/指定角度设零 | 串口命令已有（logid164），上位机无按钮 | 高 |
| 9 | **<font color=red>数据导出</font>** — 波形CSV导出、通信日志保存 | 已实现（Save Log + CAN CSV导出） | 中 |
| 10 | **<font color=red>多电机管理</font>** — 多ID设备切换/批量配置（12关节） | 未实现 | 高 |
| 11 | **<font color=red>通信超时检测</font>** — 断线检测+自动重连+超时保护 | 未实现 | 高 |
| 12 | **<font color=red>Kp/Kd/Ki范围可设定</font>** — MIT模式和位置速度模式的增益范围自定义 | 未实现（当前滑动条范围固定） | 中 |

---

## 差距总结

### 已满足（无需开发）
- CANFD接收显示、串口开关、编码器校准、OTA升级、速度模式控制、波形曲线显示、PID在线调参、故障诊断、Flash管理、AutoTune、Bode图、数据导出

### 需要新增开发（标红项汇总）

| 优先级 | 功能模块 | 工作量估计 |
|--------|----------|-----------|
| P0 | MIT阻抗控制模式面板（Kp/Kd/位置/速度/转矩） | 中 |
| P0 | CAN通用帧发送面板（ID/数据/定时/递增） | 中 |
| P0 | 多电机CAN_ID管理（12关节切换） | 大 |
| P1 | CANFD采样点+报文长度配置 | 小 |
| P1 | 串口HEX收发+队列发送 | 小 |
| P1 | 电机参数独立显示面板（含Kt） | 小 |
| P1 | 硬件型号→参数自动映射 | 中 |
| P1 | 位置速度联合模式面板 | 中 |
| P2 | 串口数据位/校验位/停止位配置 | 小 |
| P2 | 定时发送功能（ms级通用） | 小 |
| P2 | 增益范围自定义（滑动条上下限可配） | 小 |
| P2 | 抱闸/限位/零点设置按钮 | 小 |
| P2 | 温度监控面板 | 小 |
| P2 | 通信超时检测+自动重连 | 中 |
